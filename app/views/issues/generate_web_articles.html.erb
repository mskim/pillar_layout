<div class="container-fluid">
  <h2 class="text-center">인터넷 기사</h2>
  <nav>
    <div class="nav nav-tabs justify-content-center" id="nav-tab" role="tablist">
      <a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-selected="true">지면에 기제된 기사</a>
      <a class="nav-item nav-link" id="nav-profile-tab" data-toggle="tab" href="#nav-profile" role="tab" aria-controls="nav-profile" aria-selected="false">기제 안된 기사</a>
    </div>
  </nav>
  <div class="tab-content" id="nav-tabContent">
    <div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
      <div class="row">
        <div class="col-3">
          <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">
            <% @categories.each_with_index do |category, i| %>
              <a class="nav-link<% if i == 0 %> active<% end %>" id="v-pills-<%= i %>-tab" data-toggle="pill" href="#v-pills-<%= i %>" role="tab" aria-controls="v-pills-<%= i %>" aria-selected="<% if i == 0 %>true<% else %>false<% end %>"><%= category.section_name %></a>
            <% end %>
          </div>
        </div>
        <div class="col-9">
          <div class="tab-content" id="v-pills-tabContent">
            <!-- 1면 탭 -->
            <div class="tab-pane fade show active" id="v-pills-0" role="tabpanel" aria-labelledby="v-pills-0-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "1면" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 금융 탭 -->
            <div class="tab-pane fade" id="v-pills-1" role="tabpanel" aria-labelledby="v-pills-1-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "금융" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 오피니언 탭 -->
            <div class="tab-pane fade" id="v-pills-2" role="tabpanel" aria-labelledby="v-pills-2-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "오피니언" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 자치행정 탭 -->
            <div class="tab-pane fade" id="v-pills-3" role="tabpanel" aria-labelledby="v-pills-3-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "자치행정" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 정치 탭 -->
            <div class="tab-pane fade" id="v-pills-4" role="tabpanel" aria-labelledby="v-pills-4-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "자치행정" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 국제통일 탭 -->
            <div class="tab-pane fade" id="v-pills-5" role="tabpanel" aria-labelledby="v-pills-5-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "국제통일" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 전면광고 탭 -->
            <div class="tab-pane fade" id="v-pills-6" role="tabpanel" aria-labelledby="v-pills-6-tab">
              <table class="table table-dark table-bordered table-sm">
                <thead>
                  <tr>
                    <th>웹에 전송</th>
                    <th>ID</th>
                    <th>제목</th>
                    <th>저자</th>
                    <th>액션</th>
                  </tr>
                </thead>
                <tbody>
                  <% @stories.each do |story| %>
                    <% if story.category_name == "전면광고" && story.category_name.present? %>
                      <tr>
                        <%= form_for story do |f| %>
                          <td><%= f.check_box :selected_for_web, {checked: true}, true, false %></td>
                        <% end %>
                        <td><%= story.id %></td>
                        <td><%= story.title %></td>
                        <td><%= story.reporter %></td>
                        <td><%#= link_to '편집', edit_story_path(story), class: "btn btn-info" %></td>
                      </tr>
                    <% end %>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- 임시제작 -->
            <div class="tab-pane fade" id="v-pills-settings" role="tabpanel" aria-labelledby="v-pills-settings-tab">
            </div>
            <!-- 임시제작 -->
            <div class="tab-pane fade" id="v-pills-settings" role="tabpanel" aria-labelledby="v-pills-settings-tab">
            </div>
            <!-- 임시제작 -->
            <div class="tab-pane fade" id="v-pills-settings" role="tabpanel" aria-labelledby="v-pills-settings-tab">
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile-tab">...</div>
  </div>
</div>
