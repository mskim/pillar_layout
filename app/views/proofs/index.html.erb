<h1>Proofs</h1>

<table class="responsive nowrap table table-hover" id="dttb-proofs" data-sort="true" data-source="<%= url_for(format: :json)%>">
  <thead>
    <tr>
      <th data-data="working_article">Working article</th>
      <th data-data="url" data-orderable="false" data-class-name="all" data-searchable="false" class="skip-export" width="100px"></th>
    </tr>
  </thead>

</table>

<br>

<%= link_to 'New Proof', new_proof_path %>